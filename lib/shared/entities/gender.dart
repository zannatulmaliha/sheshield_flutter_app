/// Lives here (not in presentation) so no display strings are attached —
/// localized labels are mapped from this at the UI layer only, via the
/// app_en.arb / app_bn.arb keys. Adding a language never touches this file.
enum Gender { female, male, other, preferNotToSay }
